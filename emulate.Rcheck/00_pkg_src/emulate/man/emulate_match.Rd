% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emulate_match.R
\name{emulate_match}
\alias{emulate_match}
\title{Propensity score matching for target trial emulation}
\usage{
emulate_match(
  obj,
  match_cov,
  match_method = "nearest",
  match_ratio = 5L,
  caliper = 0.2,
  ps_method = "glm",
  estimand_match = "ATT",
  outcome_cov = NULL,
  model = "logistic",
  followup_spec = "quadratic",
  trial_period_spec = "quadratic",
  cluster = NULL,
  level = 95,
  quiet = FALSE
)
}
\arguments{
\item{obj}{A \code{emulate} object returned by \code{\link{emulate_expand}}.
The \code{expanded} state flag must be \code{TRUE}.}

\item{match_cov}{A character vector of covariate names for propensity score
estimation. Required.}

\item{match_method}{Matching method passed to \code{MatchIt::matchit()}.
Default is \code{"nearest"} (nearest-neighbor).}

\item{match_ratio}{Number of control matches per treated unit. Default is
\code{5L} (5:1 matching).}

\item{caliper}{Caliper width in standard deviations of the propensity score
(logit scale). Default is \code{0.2}.}

\item{ps_method}{Propensity score estimation method: \code{"glm"} (default)
or \code{"lasso"}.}

\item{estimand_match}{Estimand for matching: \code{"ATT"} (default) or
\code{"ATE"}.}

\item{outcome_cov}{Optional character vector of covariates for the outcome
model.}

\item{model}{Outcome model type: \code{"logistic"} (default), \code{"cox"},
or \code{"poisson"}.}

\item{followup_spec}{How follow-up time enters the model. Default
\code{"quadratic"}.}

\item{trial_period_spec}{How trial period enters the model. Default
\code{"quadratic"}.}

\item{cluster}{Clustering variable for robust SEs. Default: ID variable.}

\item{level}{Confidence level as percentage. Default \code{95}.}

\item{quiet}{Suppress messages. Default \code{FALSE}.}
}
\value{
The input \code{emulate} object with:
\itemize{
\item \code{state$matched} set to \code{TRUE}
\item \code{state$fitted} set to \code{TRUE}
\item \code{matching} slot populated with MatchIt object and PS values
\item \code{model} slot populated with fitted outcome model
}
}
\description{
An alternative to inverse probability weighting that matches treated and
control observations on propensity scores. Matching subsets the data to
matched pairs (or sets) and fits the outcome model on the matched sample
without IP weights.
}
\section{How matching works}{

\enumerate{
\item Propensity scores are estimated on the expanded data using logistic
regression or lasso (via \code{ps_method}).
\item \code{MatchIt::matchit()} performs nearest-neighbor matching on the
propensity score (logit scale by default).
\item The matched data is extracted and the outcome model is fitted on it.
}
}

\section{Estimand}{

By default, matching estimates the ATT (Average Treatment Effect on the
Treated). The \code{estimand_match} argument controls this. Note that this
is the matching estimand, which may differ from the trial estimand (ITT/PP/AT)
set in \code{\link{emulate_prepare}}.
}

\seealso{
\code{\link{emulate_weight}} for the IPTW alternative,
\code{\link{emulate_stratify}} for stratification.
}
